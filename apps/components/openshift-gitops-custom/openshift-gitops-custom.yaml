---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: openshift-gitops-custom
  namespace: openshift-gitops
  labels:
    app-type: config
    argoproj.io/sync-wave: "0"
spec:
  project: default
  
  source:
    repoURL: https://github.com/rajinator/k8s-apps-repo
    targetRevision: HEAD
    path: ocp/openshift-gitops-operator/overlays/custom
  
  destination:
    name: in-cluster
    namespace: openshift-gitops
  
  syncPolicy:
    automated:
      prune: false
      selfHeal: true
    syncOptions:
      - CreateNamespace=false
    retry:
      limit: 3
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

